services:
  nginx:
    image: ghcr.io/rwellinger/aiwebui-app:v2.4.1
    container_name: forward-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs/webui.crt:/etc/ssl/certs/webui.crt:ro
      - ./certs/webui.key:/etc/ssl/private/webui.key:ro
      - ./logs:/var/log/nginx
    networks:
      - thwelly-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/nginx_status"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  thwelly-net:
    external: true
    name: thwelly-net
